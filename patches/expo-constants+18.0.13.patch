diff --git a/node_modules/expo-constants/ios/EXConstants.podspec b/node_modules/expo-constants/ios/EXConstants.podspec
index fc4608a..4334325 100644
--- a/node_modules/expo-constants/ios/EXConstants.podspec
+++ b/node_modules/expo-constants/ios/EXConstants.podspec
@@ -37,7 +37,7 @@ Pod::Spec.new do |s|
   env_vars = ENV['PROJECT_ROOT'] ? "PROJECT_ROOT=#{ENV['PROJECT_ROOT']} " : ""
   script_phase = {
     :name => 'Generate app.config for prebuilt Constants.manifest',
-    :script => "bash -l -c \"#{env_vars}$PODS_TARGET_SRCROOT/../scripts/get-app-config-ios.sh\"",
+    :script => "echo \"DEBUG: PODS_TARGET_SRCROOT=$PODS_TARGET_SRCROOT\"; bash -c \"#{env_vars}\\\"$PODS_TARGET_SRCROOT/../scripts/get-app-config-ios.sh\\\"\"",
     :execution_position => :before_compile
   }
   # :always_out_of_date is only available in CocoaPods 1.13.0 and later
diff --git a/node_modules/expo-constants/scripts/get-app-config-ios.sh b/node_modules/expo-constants/scripts/get-app-config-ios.sh
index b612e09..97de0c9 100755
--- a/node_modules/expo-constants/scripts/get-app-config-ios.sh
+++ b/node_modules/expo-constants/scripts/get-app-config-ios.sh
@@ -1,4 +1,6 @@
 #!/usr/bin/env bash
+echo "DEBUG: Running get-app-config-ios.sh"
+set -x
 
 set -eo pipefail
 
@@ -32,4 +34,4 @@ else
   exit 1
 fi
 
-"${EXPO_CONSTANTS_PACKAGE_DIR}/scripts/with-node.sh" "${EXPO_CONSTANTS_PACKAGE_DIR}/scripts/getAppConfig.js" "$PROJECT_ROOT" "$RESOURCE_DEST"
+"${EXPO_CONSTANTS_PACKAGE_DIR}/scripts/with-node.sh" "${EXPO_CONSTANTS_PACKAGE_DIR}/scripts/getAppConfig.js" "$PROJECT_ROOT" "$RESOURCE_DEST" > /tmp/expo-constants-debug.log 2>&1
diff --git a/node_modules/expo-constants/scripts/with-node.sh b/node_modules/expo-constants/scripts/with-node.sh
index 41b6b3b..2daa069 100755
--- a/node_modules/expo-constants/scripts/with-node.sh
+++ b/node_modules/expo-constants/scripts/with-node.sh
@@ -11,8 +11,13 @@ CURR_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
 
 # Start with a default
 NODE_BINARY=$(command -v node)
+NODE_BINARY=$(command -v node)
 export NODE_BINARY
 
+echo "DEBUG: Starting with-node.sh" > /tmp/expo-debug-env.log
+echo "DEBUG: PODS_ROOT=$PODS_ROOT" >> /tmp/expo-debug-env.log
+env >> /tmp/expo-debug-env.log
+
 # Override the default with the global environment
 ENV_PATH="$PODS_ROOT/../.xcode.env"
 if [[ -f "$ENV_PATH" ]]; then
